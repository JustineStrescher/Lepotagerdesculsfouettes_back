<form class="mb-3" id="back_delete_button"  method="post" action="{{ path('back_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}"><br/>
    <button class="btn btn-outline-danger  d-grid gap-2 col-6 mx-auto">Supprimer</button><br/>
</form>
