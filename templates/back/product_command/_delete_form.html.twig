<form method="post" action="{{ path('back_product_command_delete', {'id': product_command.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_command.id) }}">
    <button class="btn">Delete</button>
</form>
