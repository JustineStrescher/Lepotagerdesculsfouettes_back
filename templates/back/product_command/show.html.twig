{% extends 'base.html.twig' %}

{% block title %}ProductCommand{% endblock %}

{% block body %}
    <h1>ProductCommand</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_command.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product_command.quantity }}</td>
            </tr>
            <tr>
                <th>UnitPrice</th>
                <td>{{ product_command.unitPrice }}</td>
            </tr>
            <tr>
                <th>TotalTTC</th>
                <td>{{ product_command.totalTTC }}</td>
            </tr>
            <tr>
                <th>TotalHT</th>
                <td>{{ product_command.totalHT }}</td>
            </tr>
            <tr>
                <th>TotalTVA</th>
                <td>{{ product_command.totalTVA }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_product_command_index') }}">back to list</a>

    <a href="{{ path('back_product_command_edit', {'id': product_command.id}) }}">edit</a>

    {{ include('back/product_command/_delete_form.html.twig') }}
{% endblock %}
