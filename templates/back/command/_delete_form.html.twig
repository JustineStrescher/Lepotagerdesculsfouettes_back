<form class="mb-3" id="back_delete_button" method="post" action="{{ path('back_command_delete', {'id': command.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette commande?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ command.id) }}">
    <button class="btn btn-outline-danger  d-grid gap-2 col-6 mx-auto" >Supprimer</button>
</form>
